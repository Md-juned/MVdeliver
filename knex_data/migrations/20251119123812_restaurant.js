/**
 * This migration file was restored to satisfy Knex's migration list.
 * The original changes have already been applied in the target database,
 * so the up/down handlers are intentionally left as no-ops.
 *
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
export async function up(_knex) {
  // no-op: original migration already applied; file restored for integrity
}

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
export async function down(_knex) {
  // no-op
}

